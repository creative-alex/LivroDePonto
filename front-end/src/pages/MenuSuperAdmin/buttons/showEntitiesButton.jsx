import React from 'react';

const ShowEntites = ({ onClick }) => {
  return (
    <button class="show-ent" onClick={onClick}>
      Mostrar Entidades & Users
    </button>
  );
};

export default ShowEntites;